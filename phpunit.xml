<phpunit bootstrap="autoload.php">
    <testsuites>
        <testsuite name="alcon">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <filter>
        <whitelist processUncoveredFilesFromWhitelist="true">
            <directory suffix=".php">tests</directory>
            <exclude>
            </exclude>
        </whitelist>
    </filter>
</phpunit>
